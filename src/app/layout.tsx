import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DocuCollect - Document Collection for Accountants",
  description: "Stop chasing clients for documents. The simple document collection portal built for accountants. Send requests, get automatic reminders, and receive files securely.",
  keywords: ["document collection", "accountants", "tax documents", "CPA", "client portal", "file sharing"],
  openGraph: {
    title: "DocuCollect - Document Collection for Accountants",
    description: "Stop chasing clients for documents. The simple document collection portal built for accountants.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
